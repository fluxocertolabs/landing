# FluxoCerto — Landing Page

Landing page promocional (pt-BR) para o produto **FluxoCerto**.

## Requisitos
- Node.js >= 20
- pnpm >= 10

## Rodar localmente
```bash
pnpm install
pnpm dev
```

## Configuração
Crie um `.env` (opcional):
```bash
VITE_APP_URL=https://seu-app.com
```
